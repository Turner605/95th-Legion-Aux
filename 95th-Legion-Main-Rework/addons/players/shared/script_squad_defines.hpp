#if SQUAD_KEY == Zeus
    #define SQUAD_NUMBER 0-1
    #define SQUAD_NAME Zeus
#endif

#if SQUAD_KEY == Fallen
    #define SQUAD_NUMBER 0-0
    #define SQUAD_NAME Fallen
#endif

#if SQUAD_KEY == Unassigned
    #define SQUAD_NUMBER 0-0
    #define SQUAD_NAME Unassigned
#endif

#if SQUAD_KEY == Basic
    #define SQUAD_NUMBER 0-0
    #define SQUAD_NAME Basic
#endif

#if SQUAD_KEY == One_Zero
    #define SQUAD_NUMBER 1-0
    #define SQUAD_NAME Anooba
#endif

#if SQUAD_KEY == One_One
    #define SQUAD_NUMBER 1-1
    #define SQUAD_NAME Ackley
#endif

#if SQUAD_KEY == One_Two
    #define SQUAD_NUMBER 1-2
    #define SQUAD_NAME Reek
#endif

#if SQUAD_KEY == One_Three
    #define SQUAD_NUMBER 1-3
    #define SQUAD_NAME Krayt
#endif

#if SQUAD_KEY == One_Four
    #define SQUAD_NUMBER 1-4
    #define SQUAD_NAME Sarlacc
#endif

#if SQUAD_KEY == Five_Zero
    #define SQUAD_NUMBER 5-0
    #define SQUAD_NAME Zillo
#endif

#if SQUAD_KEY == Six_Zero
    #define SQUAD_NUMBER 6-0
    #define SQUAD_NAME Mynock
#endif

#if SQUAD_KEY == Seven_Zero
    #define SQUAD_NUMBER 7-0
    #define SQUAD_NAME Banshee
#endif